<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lmpjt.pilotpjt.dao.BoardDAO">

	<select id="boardView"
		resultType="com.lmpjt.pilotpjt.dto.BoardDTO">
		select boardNumber, userNumber, userName, boardTitle, boardContent,
		boardWriteDate, boardHit, boardViews, boardLikes
		from board
	</select>

	<insert id="boardWrite">
		insert into board(boardNumber, userNumber, userName,
		boardTitle, boardContent)
		values ((SELECT NVL(MAX(boardNumber), 0) + 1
		FROM board), #{userNumber}, #{userName}, #{boardTitle}, #{boardContent})
	</insert>

	<select id="boardDetailView"
		resultType="com.lmpjt.pilotpjt.dto.BoardDTO">
		select boardNumber, userNumber, boardTitle, boardContent,
		boardWriteDate, boardHit, boardViews, boardLikes, userName
		from board where
		boardNumber=#{boardNumber}
	</select>

	<!-- void 일 경우 resultType도 없음 -->
	<update id="boardModify">
		update tbl_board set boardName=#{boardName},
		boardTitle=#{boardTitle}, boardContent=#{boardContent} where
		boardNo=#{boardNo}
	</update>

	<update id="boardDelete">
		delete from tbl_board where boardNo=#{boardNo}
	</update>

</mapper>