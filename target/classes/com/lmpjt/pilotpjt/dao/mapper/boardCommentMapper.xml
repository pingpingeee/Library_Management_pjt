<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lmpjt.pilotpjt.dao.BoardCommentDAO">

	<select id="bcView"
		resultType="com.lmpjt.pilotpjt.dto.BoardCommentDTO">
		select commentNumber, boardNumber, userNumber, userName,
		commentContent, commentWriteDate from board_comment where boardNumber=#{boardNumber}
	</select>

	<insert id="bcWrite">
		insert into board_comment (commentNumber,
		boardNumber, userNumber, userName, commentContent) values ((SELECT
		NVL(MAX(commentNumber), 0) + 1
		FROM board_comment), #{boardNumber},
		#{userNumber}, #{commentContent})
	</insert>

	<update id="bcModify">
		update board_comment set commentNumber=#{commentNumber},
		boardNumber=#{boardNumber}, userNumber=#{userNumber}, 
		userName=#{userName}, commentContent=#{commentContent} 
		where commentNumber=#{commentNumber}
	</update>

	<update id="boardDelete">
		delete from board_comment where commentNumber=#{commentNumber}
	</update>


</mapper>